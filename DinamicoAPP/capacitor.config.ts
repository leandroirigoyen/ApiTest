import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dinamico.app',
  appName: 'myfirstionicproject',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
